export default function writeEffect(element) {
  const typed = new Typed(element, {
    strings: [" Cristian Meneses"],
    typeSpeed: 85,
    startDelay: 350,
    backSpeed: 85,
    smartBackspace: true,
    backDelay: 1000,
    loop: true,
    loopCount: false,
    showCursor: true,
    contentType: "html",
  });
}
